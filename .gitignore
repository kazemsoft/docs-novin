node_modules/
build/
.docusaurus/
.cache-loader/
.env
.env.local
.DS_Store
npm-debug.log*
yarn-error.log*

# Vercel CLI (contains project/org ids)
.vercel/
.vercel
.env*

# Stray personal/scratch markdown dropped in the repo root.
# origin is a PUBLIC repo, so root-level notes are ignored by default and the
# real docs are re-allowed explicitly. Docs live in docs/, never in the root.
/*.md
!/README.md
!/CONTRIBUTING.md
!/DEPLOYMENT.md
