# راهنمای مشارکت در مستندات نوین کلاود

مستندات نوین کلاود متن‌باز است و از مشارکت شما استقبال می‌کنیم. اگر غلط املایی، توضیح نامفهوم، مثال اشتباه یا مطلبی جامانده دیدید، می‌توانید مستقیماً آن را اصلاح کنید.

## راه اول — ویرایش در مرورگر (ساده‌ترین راه)

برای اصلاح‌های کوچک نیازی به نصب هیچ ابزاری نیست:

۱. به صفحه‌ای که می‌خواهید اصلاح کنید بروید.
۲. در پایین صفحه روی **«ویرایش این صفحه»** کلیک کنید.
۳. گیت‌هاب فایل را باز می‌کند. روی آیکون مداد (✏️) بزنید.
۴. تغییر خود را اعمال کنید.
۵. پایین صفحه، توضیح کوتاهی از تغییر بنویسید و روی **Propose changes** بزنید.
۶. روی **Create pull request** کلیک کنید.

همین. ما درخواست شما را بررسی می‌کنیم.

> اگر حساب گیت‌هاب ندارید، ساخت آن رایگان است و کمتر از یک دقیقه طول می‌کشد.

## راه دوم — اجرای محلی (برای تغییرات بزرگ‌تر)

```bash
git clone https://github.com/novincloud/docs.git
cd docs
npm install
npm start
```

سایت روی `http://localhost:3000` بالا می‌آید و با ذخیره هر فایل، بلافاصله به‌روز می‌شود.

پیش از ارسال، از سالم بودن build مطمئن شوید:

```bash
npm run build
```

## ساختار پروژه

```
docs/                  فایل‌های مستندات (Markdown)
├── getting-started/   صفحات مقدماتی
└── ai/                مستندات هوش مصنوعی
sidebars.ts            ساختار منوی کناری
docusaurus.config.ts   تنظیمات سایت
src/css/custom.css     استایل و فونت
```

## افزودن صفحه جدید

۱. فایل `.md` را در پوشه مناسب بسازید.
۲. در ابتدای فایل، frontmatter را بنویسید:

```markdown
---
id: my-page
title: عنوان صفحه
sidebar_label: عنوان منو
sidebar_position: 3
description: توضیح کوتاه یک‌خطی برای موتورهای جستجو.
---
```

۳. شناسه صفحه را در `sidebars.ts` اضافه کنید تا در منو دیده شود.

## راهنمای نگارش

**زبان ساده بنویسید.** مخاطب ممکن است تازه‌کار باشد. از اصطلاح تخصصی بدون توضیح استفاده نکنید.

**فارسی را درست بنویسید.** از نیم‌فاصله در جای درست استفاده کنید (`می‌شود` نه `میشود`، `کلیدها` نه `کلید ها`).

**نمونه کد را تست کنید.** هر کد نمونه‌ای که می‌نویسید باید واقعاً کار کند.

**کلید واقعی در کد نگذارید.** همیشه از `sk-xxxxxxxx` یا متغیر محیطی استفاده کنید.

**کد و خروجی را در بلوک کد بگذارید** و زبان آن را مشخص کنید:

````markdown
```python
print("سلام")
```
````

**از کادرهای هشدار استفاده کنید:**

```markdown
:::tip
نکته مفید
:::

:::warning
هشدار
:::

:::danger
خطر — عمل برگشت‌ناپذیر
:::
```

**لینک‌های داخلی را نسبی بنویسید:**

```markdown
[کلیدهای API](./api-keys.md)
```

## چه چیزهایی را گزارش کنیم؟

- ❌ غلط املایی و نگارشی
- ❌ کد نمونه‌ای که کار نمی‌کند
- ❌ اطلاعات قدیمی یا نادرست
- ❌ توضیح مبهم
- ➕ مطلبی که جا افتاده
- ➕ مثال کاربردی بیشتر

## گزارش مشکل بدون ارسال کد

اگر وقت یا تمایل ارسال Pull Request ندارید، کافی است یک [Issue](https://github.com/novincloud/docs/issues/new) باز کنید و مشکل را توضیح دهید. همین هم کمک بزرگی است.

## سوالات مربوط به سرویس

این مخزن فقط برای **مستندات** است. اگر مشکل فنی در خود سرویس دارید یا سوال مربوط به حساب کاربری‌تان است، از [بخش پشتیبانی کنسول](https://console.novin.cloud/dashboard/tickets) تیکت ثبت کنید.

---

از مشارکت شما سپاسگزاریم. 🙏
